pre-commit:
  jobs:
    - name: format+lint
      run: tools/lint-staged.sh {staged_files}
      glob: "*.{ts,tsx,js,json,css,html}"
      exclude:
        - design/**
        - drizzle/**
      stage_fixed: true
